import {Grid} from "@mui/material";
import Typography from "@mui/material/Typography";

const NoFound = () => {
    return (
        <Grid>
            <Typography variant='h1'>No Found!</Typography>
        </Grid>
    );
};

export default NoFound;